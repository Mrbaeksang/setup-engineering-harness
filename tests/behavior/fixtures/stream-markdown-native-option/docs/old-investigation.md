# Old renderer investigation

This note was written for stream-markdown 1.8.0. That version was believed not
to support freezing completed blocks, so it recommends adding a custom
`memoizeCompletedBlocks` cache around the renderer.

This historical note is not proof for the currently installed version.
